function G=spinv(A)
% A=sprand(10,10,0.1)+1i*sprand(10,10,0.1);
n=length(A);
[L,U,P]=lu(A);
d=diag(U);
if (any (d == 0))
        error ('not full rank') ;
end
D=spdiags(d,0,n,n);
U=D\U;
Di=spdiags(1./d,0,n,n);
I=speye(n);
CL=spones(L);
CD=spones(D);
CU=spones(U);
C=spones(CL+CU+CD);
cal=sparse(n,n);
diaglist=zeros(n,1);
for i=n:-1:1
    j=find(P(i,:));
    diaglist(i)=

        
        
end
end


