function zc=zcfun(i,j,zc,C,n)
if ~(zc(i,j)) %have calculated
    if (i<=j)
        zc(i+1:n,j)=zc(i+1:n,j)|C(i,i+1:n)';
        for k=i+1:n
            if 
            zc=zcfun(k,j,zc,C,n);
        end
    else
        zc(i,j+1:n)=zc(i,j+1:n)|C(j+1:n,j)';
        for k=j+1:n
            zc=zcfun(i,k,zc,C,n);
        end
    end        
end
end